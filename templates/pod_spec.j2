containers:
    - name: {{ name }}
      imageDetails:
          imagePath: {{ registry_path }}
          username: {{ image_username }}
          password: {{ image_password }}
      ports:
      	- name: ssh
	  containerPort: 22
